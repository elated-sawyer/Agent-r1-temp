
2026-04-22

Now My mentor ask me to collect sft data to training the agent
we would collect high quality trajectories generated by policy model itself as the sft data, since the trajectories generated by other models would mismatch the policy model's, which could have damages on the policy model training.

I created a new branch, e.g., 'sft_data_collection'. there are several functions need to be improved or added to the current project (sft_data_collection branch).
- increase the number of "actor_rollout_ref.rollout.val_kwargs.n", e.g., to 8 or 16, more rollouts is more likely to have more probabilities to get high quality trajectoires.
- save the success trajectories (find the retrosynthesis path successfuly), if there are multiple success trajs in a query, just save the top 2 shortest trajectories.
- since the saved traj data would be used for SFT in the future, so the data should adopt a standard training data format, e.g., 
  - two columns, (1) id (indicating some generation info) (2) conversations (e.g., [{"role": "system","content": "..."},{"role": "user","content": "..."},{"role": "assistant","content": "..."},...])
- set saving frequency, e.g., per 50 steps, and also should have a resume machanism, continue with the last data collection checkpoint
- save all the collect data in "data_sft"
- the name of the saved sft data, should include essntial info, e.g., saving steps, force_noloop, 
- modify @Agent-r1-temp/test.sbatch , change to use training data @Agent-r1-temp/data/reaction_pathway_search/train_h4_10.parquet.
